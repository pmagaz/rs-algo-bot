apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodb-bot.fullname" . }}-test-connection"
  labels:
    {{- include "mongodb-bot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodb-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: OnFailure
